/**
 * app.js
 * ------
 * À QUOI ÇA SERT :
 * Tes interactions jQuery propres au projet (appels AJAX, validation de
 * formulaire côté client...). Chargé en bas de page (voir footer.php),
 * après jQuery et Bootstrap JS, donc $ est déjà disponible ici.
 *
 * EXEMPLE (à décommenter/adapter quand tu en as besoin) :
 *
 * $(function () {
 *     $('#nom').on('blur', function () {
 *         if ($(this).val().trim() === '') {
 *             $(this).addClass('is-invalid');
 *         }
 *     });
 * });
 */
